---
'@k8o/arte-odyssey': major
---

Add new `CheckboxGroup` and `RadioGroup` form components with docs and
Storybook coverage.

Allow `Checkbox` to be used directly inside `CheckboxGroup` so grouped
checkboxes share the same visual API as standalone checkboxes.

Remove the standalone `Radio` public API in favor of `RadioGroup`.
