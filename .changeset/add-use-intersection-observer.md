---
'@k8o/arte-odyssey': major
---

feat: add `useIntersectionObserver` and `useInView` hooks, refactor ref-based hooks to accept ref

BREAKING CHANGE: `useClickAway` and `useResize` now accept a `ref` as the first argument instead of returning one.
