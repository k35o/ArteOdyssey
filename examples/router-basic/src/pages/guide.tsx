import type { FC } from 'react';

export const Guide: FC = () => (
  <article data-testid="guide">
    <h2>guide</h2>
    <p>this page lives under the (docs) group, but its URL is just /guide.</p>
  </article>
);
